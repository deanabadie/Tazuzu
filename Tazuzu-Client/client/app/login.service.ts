//#sourceMappingURL=login.service.js.mapimport { Injectable } from '@angular/core';
import { Component } from '@angular/core';

export class LoginService {
  constructor() { }

  getUsername(username: string | number) {
    let name = username;
    return name;
  }
}